# Filter Signals Test Fixture

Developer-only debug actions and deterministic UI fixtures for live verification. Build and load this folder separately from the shipping mod; it must never be included in a release package.
